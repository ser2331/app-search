import React from 'react';
import "./app-header.scss";

const AppHeader = () => {
    return (
        <div className="AppHeader">
            App to find people by name
        </div>
    );
};

export default AppHeader;
